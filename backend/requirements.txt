# =============================================================================
# Urban Intelligence Platform - Backend Dependencies
# =============================================================================

# Web Framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Database
sqlalchemy==2.0.36
aiosqlite==0.20.0
alembic==1.14.1

# Auth & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# Validation & Serialization
pydantic==2.10.4
pydantic-settings==2.7.1

# WebSocket
websockets==14.1

# HTTP
httpx==0.28.1

# Computer Vision
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==11.1.0

# ML (optional - install if GPU available)
ultralytics==8.3.0
# torch==2.5.0
# torchvision==0.20.0

# OCR/ANPR dependencies are intentionally kept out of the lightweight web service.
# Install them on the dedicated inference worker when ANPR is enabled.

# Utilities
python-dotenv==1.0.1
loguru==0.7.3
geopy==2.4.1
shapely==2.0.6

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1

# Reports
# reportlab==4.2.5  # For PDF generation
